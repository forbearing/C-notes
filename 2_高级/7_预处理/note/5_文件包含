1:文件包含的概念
	1:文件包含是C预处理程序的另一个重要功能,背包汗文件的名字必须用双引号或
	  一对尖括号括起来
	2:文件包含的语法
		#include <文件名>
		或者
		#include "文件名"
		1:功能: 一个源文件可将另一个源文件的内容全部包含进来,从而把指定的文件和
		  当前的源程序文件连成一个源文件
		2:处理过程: 在预处理时,用被包含文件的内容取代该文件包含指令,再对包含
		  的文件做一个源文件编译
		3:一般而言,若调用标志库函数用 #include <文件名>, 若要包含用户自己编写的
		  文件则用 #define "文件名"
		4:一个 include 指令只能指定一个被包含的文件, 允许嵌套包含,被包含的文件
		  可以是源文件(*.c) 或者头文件(*.h)
	3:文件包含的搜索方式
		1:#include <文件名>
			1:若指定文件目录(如#Include)则从此目录中找,否则按标准方式查找
			2:标准方式: 从系统标准文件所在的目录中寻找要包含的文件
		2:#define "文件名"
			1:先从存放C源文件的目录中查找,然后若用户指定目录(如include), 再从此
			  目录中寻找要包含的文件,若找不到再按标准方式查找
	4:文件包含的作用
		1:一个大的程序可以分成多个模块,由多个程序员分别编程. 有些共用的符号常量
		  结构体声明或宏定义等可单独组成一个文件, 在其它文件的开头用包含指令包含
		  该文件即可使用
		2:可避免在每个文件开头都去书写那些公用量,从而节省时间,并减少出错


2:多重包含
	1:同一个文件被包含多次称为多重包含
	2:多重包含可能会出现重复定义的编译错误
	3:为了防止多重包含可使用条件编译
		#ifndef __HEADERNAME_H__
		#define __HEADERNAME_H__ 1
		#include "headername.h"
		#endif
	4:条件编译只能解决同一个文件中的多重包含,不能解决多个文件中包含同一个头文件
	  而出现的多重包含错误




示例:
	demo.h
	demo_test.c
		gcc -E -o demo_test.i src/demo_test.c -Iinclude
	
	common.h
	a.h
	b.h
	mul.c
